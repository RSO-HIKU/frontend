name: Frontend Web Deploy (test)

on:
  push:
    branches: [ test ]
  pull_request:
    branches: [ test ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    environment: test
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install
        run: npm ci

      - name: Export web build (dist/)
        env:
          EXPO_PUBLIC_API_BASE_URL: ${{ vars.EXPO_PUBLIC_API_BASE_URL }}
          EXPO_PUBLIC_KEYCLOAK_URL: ${{ vars.EXPO_PUBLIC_KEYCLOAK_URL }}
          EXPO_PUBLIC_KEYCLOAK_REALM: ${{ vars.EXPO_PUBLIC_KEYCLOAK_REALM }}
          EXPO_PUBLIC_KEYCLOAK_CLIENT_ID: ${{ vars.EXPO_PUBLIC_KEYCLOAK_CLIENT_ID }}
          EXPO_PUBLIC_ENV: ${{ vars.EXPO_PUBLIC_ENV }}
        run: |
          npm run web:export
          test -d dist

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: web-dist
          path: dist

  deploy:
    if: github.event_name == 'push'
    needs: build
    runs-on: ubuntu-latest
    environment: test
    env:
      STORAGE_ACCOUNT_NAME: ${{ secrets.STORAGE_ACCOUNT_NAME }}
      AZURE_STORAGE_KEY: ${{ secrets.AZURE_STORAGE_KEY }}
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: web-dist
          path: dist

      - name: Deploy to Azure Storage Static Website ($web)
        run: |
          # Remove old files to avoid stale hashed bundles building up
          az storage blob delete-batch --account-name "$STORAGE_ACCOUNT_NAME" --source '$web' || true
          az storage blob upload-batch --account-name "$STORAGE_ACCOUNT_NAME" --destination '$web' --source dist --overwrite
